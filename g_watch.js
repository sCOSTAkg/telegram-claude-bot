const fs=require('fs'),{execSync}=require('child_process'),folder='/Users/guest1/Desktop/sCORP',apiKey=process.env.GEMINI_API_KEY;fs.watch(folder,(e,f)=>{if(f&&f.endsWith('.ogg')){const p=`${folder}/${f}`;if(fs.existsSync(p)){try{const b=execSync(`base64 -i "${p}"`).toString().trim(),r=execSync(`curl -s -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}" -H "Content-Type: application/json" -d '{"contents":[{"parts":[{"text":"Transcribe to Russian"},{"inline_data":{"mime_type":"audio/ogg","data":"' + b + '"}}]}]}'`).toString();fs.writeFileSync(`${folder}/last_voice.txt`,JSON.parse(r).candidates[0].content.parts[0].text)}catch(err){}}}});